# Buff 被地图缩放及留边裁剪

2026-09-15。修复基于已验收的 PR #12；本次只修正世界图层与屏幕界面图层的分类。

以下倍率和前后对照为当时修复记录。2026-09-16 的人物比例调整见
[当前取景规则](map-magnification-samples.md)，Buff 独立于世界裁剪的修复继续保留。

## 原因与处理

原生 TemporaryStatView 的图标和冷却遮罩均使用根图层 Z `0xC006156C`。
支持的 v83 EXE 在 `0x007B406D`、`0x007B4310` 分别压入该值，再创建两个图层。
它比底栏 Z `0xC00615D0` 低 100。此前仅以“底栏以下”为世界图层的判定，
会把 Buff 送入地图投影和地图裁剪矩形。

现在排除这一明确的屏幕图层 Z。仍先沿 overlay 父链找到根图层，保证
Buff 的附属图层跟随 HUD，人物的附属图层跟随人物。保留其他场景特效的
原有分类，不整体降低世界图层的 Z 上限。

Buff 使用完整渲染画面的右上角坐标；地图的放大倍率、相机、左右留边和
高窄图 4:3 规则不变。大木林Ⅰ的额外世界倍率为 1，仍可复现裁剪，说明
单纯限制放大倍率不能解决这次问题。底栏和外层窗口缩放也不改。

原有图标、冷却层、FindIcon 和 ShowToolTip 分辨率补丁已使用屏幕尺寸。
原生提示和右键路径以 `GetCursorPos(bWorld=0)` 查询光标，绕过世界坐标
反变换；本次无需移动图标坐标或改动鼠标挂钩。

## 同一组件的修复前后对照

用真实 PCOM / Gr2D / Canvas 和 research 当前的 D3D8-to-9 模块回读 GPU
像素。三个 32×32 色块代表最右图标、下一图标和单独的冷却根图层，
均使用上述原生 Z；下一图标放在 x=1853，跨过大木林Ⅰ的 x=1860 裁剪线。
同时绘制原始 NPC 2140000、400 个相邻纹理及 1200×40 底栏。
这是实际图形组件测试，未替代游戏内 Buff 技能、悬停、取消操作的验收。

| 地图 | 世界倍率 | 修复前可见宽度：最右 / 下一 / 冷却 | 修复后 |
|---|---:|---:|---|
| 270010000 神录的过去 | 1.451613 | 0 / 0 / 0 | 三者均完整 32×32 |
| 101010100 大木林Ⅰ | 1.000000 | 0 / 7 / 32 | 三者均完整 32×32 |
| 101020001 北部森林通道 | 1.836735 | 0 / 0 / 0 | 三者均完整 32×32 |

修复前源文件取自 PR #12 合并提交 `e14d6b5`，保留同样的地图素材边界计算；
没有拿更早的地图放大规则做对照。两版 NPC 尺寸、场景裁剪及底栏像素完全一致。

运行 `tools/test-map-magnification-native.ps1 -BuffRegression` 验证修复。
历史对照时另加 `-ExpectBuffBug -ViewportSource <旧版 WorldViewport.cpp> -FixturePath <9b610e1 的 buff-hud.txt>`。
输入为 `tests/fixtures/buff-hud.txt`，CSV 和 BMP 位于 `out/world-native`。
测试需桌面 GPU，但不启动游戏，也不写客户端配置或 WZ。

其他验证：

- 原生 EXE 两个根 Z 指令签名，以及图标、冷却、提示、点击位置补丁在 64 组分辨率/布局组合下通过。
- 模拟设备验证 Buff、冷却及负 Z 子层不修改投影或裁剪；使用同一 Z 的人物子层仍按人物根图层缩放，其他场景特效分类保留；含设备失败分支。
- 18 组既有地图素材边界的原生图形回归通过，NPC 比例、底栏和相邻纹理检查保持原结果。
- 4,126 张显式地图边界的相机/鼠标变换数据检查及窗口缩放、布局、恢复等现有测试通过。

游戏验收请在以上三张地图间带 Buff 往返，复查图标、冷却显示、悬停提示、
可取消 Buff 的右键操作，以及窗口缩放、最大化和全屏切换。
